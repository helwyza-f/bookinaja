name: Fast Deploy Bookinaja Monorepo to IDCloudHost

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # --- 1. GENERATE .ENV DARI SECRETS ---
      - name: Create Environment Files
        run: |
          echo "${{ secrets.BE_ENV }}" > backend/.env
          echo "${{ secrets.FE_ENV }}" > frontend/.env

      # --- 2. BUILD FRONTEND (NEXT.JS) ---
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "frontend/package-lock.json"
      - name: Build Frontend Standalone
        env:
          NEXT_PUBLIC_API_URL: https://bookinaja.com/api/v1
        run: |
          cd frontend

          npm install
          npm run build

      # --- 3. PRE-CLEAN VPS ---
      - name: Reset Environment on VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            mkdir -p ~/bookinaja/backend ~/bookinaja/frontend
            rm -f ~/bookinaja/backend/main
            rm -f ~/bookinaja/backend/.env
            rm -f ~/bookinaja/frontend/.env
            rm -rf ~/bookinaja/frontend/.next

      # --- 4. TRANSFER FILES (SCP) ---
      - name: Transfer Files to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "backend/cmd,backend/go.mod,backend/go.sum,backend/migrations,backend/internal,backend/Dockerfile,backend/.env,frontend/.next/standalone,frontend/.next/static,frontend/public,frontend/Dockerfile,frontend/.env,docker-compose.yml"
          target: "~/bookinaja"
          strip_components: 0
          overwrite: true

      # --- 5. RESTART DOCKER ON VPS (FIXED COMMAND) ---
      - name: Execute Docker Commands
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd ~/bookinaja
            sudo docker compose down
            sudo docker compose build --no-cache backend
            sudo docker compose up -d
            sudo docker image prune -f
