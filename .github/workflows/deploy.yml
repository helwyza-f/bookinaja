name: Fast Deploy Bookinaja Monorepo to IDCloudHost

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # --- 1. GENERATE .ENV DARI SECRETS ---
      # Menghindari hardcode kredensial di repository
      - name: Create Environment Files
        run: |
          echo "${{ secrets.BE_ENV }}" > saas/.env
          echo "${{ secrets.FE_ENV }}" > saas-app/.env

      # --- 2. BUILD BACKEND (GO) ---
      # Build binary di runner GitHub agar VPS tidak terbebani proses kompilasi
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.24"
      - name: Build Backend Binary
        run: |
          cd saas
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -o main cmd/api/main.go

      # --- 3. BUILD FRONTEND (NEXT.JS) ---
      # Build standalone agar image docker sangat ringan untuk RAM 2GB
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "saas-app/package-lock.json"
      - name: Build Frontend Standalone
        env:
          NEXT_PUBLIC_API_URL: https://bookinaja.com/api/v1
        run: |
          cd saas-app
          npm install
          npm run build

      # --- 4. PRE-CLEAN VPS ---
      # Menghapus residu build lama agar disk 20GB tidak bengkak
      - name: Reset Environment on VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            mkdir -p ~/bookinaja/saas ~/bookinaja/saas-app
            rm -f ~/bookinaja/saas/main
            rm -f ~/bookinaja/saas/.env
            rm -f ~/bookinaja/saas-app/.env
            rm -rf ~/bookinaja/saas-app/.next

      # --- 5. TRANSFER FILES (SCP) ---
      # Mengirim source, binary, migrations, dan .env
      - name: Transfer Files to VPS
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "saas/main,saas/migrations,saas/internal,saas/Dockerfile,saas/.env,saas-app/.next/standalone,saas-app/.next/static,saas-app/public,saas-app/Dockerfile,saas-app/.env,docker-compose.yml"
          target: "~/bookinaja"
          strip_components: 0
          overwrite: true

      # --- 6. RESTART DOCKER ON VPS ---
      - name: Execute Docker Commands
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd ~/bookinaja
            # Membersihkan container lama sebelum rebuild
            sudo docker-compose down
            # Rebuild image lokal menggunakan binary yang sudah ditransfer
            sudo docker-compose up --build -d
            # WAJIB: Hapus dangling images agar disk VPS tidak penuh
            sudo docker image prune -f
